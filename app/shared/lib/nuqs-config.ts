// Nuqs configuration for Remix
import { useQueryState, useQueryStates } from 'nuqs';

// Re-export the hooks directly
// They will use the NuqsAdapter provided at the root level
export { useQueryState, useQueryStates };
